# Core API & Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
python-dotenv>=1.0.0

# Trading & Market Data
alpaca-py>=0.21.0
ccxt>=4.1.0
websocket-client>=1.6.0
aiohttp>=3.9.0

# Data Processing (lightweight)
numpy>=1.24.0
pandas>=2.0.0

# Machine Learning (optimized - remove TensorFlow for faster builds)
scikit-learn>=1.3.0
xgboost>=2.0.0
river>=0.21.0

# Visualization (optional - comment out if not needed)
# plotly>=5.17.0
# streamlit>=1.28.0

# Database (lightweight)
sqlalchemy>=2.0.0
redis>=5.0.0

# PostgreSQL only if you need it (comment out for faster builds)
# psycopg2-binary>=2.9.0